---
phase: 01-architecture-foundation
plan: 02
type: execute
wave: 2
depends_on: [01-01]
files_modified: [database/schema.sql, src/Core/Database.php, src/Core/BaseRepository.php]
autonomous: true
requirements: [DB-01, DB-02, DB-03]
must_haves:
  truths:
    - "Database schema can be created from database/schema.sql"
    - "src/Core/Database.php can establish a PDO connection"
  artifacts:
    - path: "database/schema.sql"
      provides: "MySQL database design"
    - path: "src/Core/Database.php"
      provides: "PDO connection abstraction"
    - path: "src/Core/BaseRepository.php"
      provides: "Base CRUD operations for repositories"
  key_links:
    - from: "src/Core/BaseRepository.php"
      to: "src/Core/Database.php"
      via: "dependency injection"
    - from: "src/Core/Database.php"
      to: "PDO"
      via: "instantiation"
---

<objective>
Design the database schema and implement a robust database abstraction layer using PDO and the Repository pattern.

Purpose: Provides a clean, typed, and secure way to interact with the MySQL database.
Output: Initial MySQL schema, PDO connection wrapper, and reusable base Repository class.
</objective>

<execution_context>
@C:/Users/Thanon6532/.gemini/get-shit-done/workflows/execute-plan.md
@C:/Users/Thanon6532/.gemini/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/research/ARCHITECTURE.md
@.planning/phases/01-architecture-foundation/01-01-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: MySQL Schema Design</name>
  <files>database/schema.sql</files>
  <action>
    1. Create `database/` directory if not exists.
    2. Design `database/schema.sql` with:
       - `users` table: id, email (unique), google_id, telegram_id, tokens (json).
       - `events` table: id, user_id (FK), title, description, start_time, end_time, google_event_id.
       - Use `InnoDB` and `utf8mb4` charset.
    3. Include necessary indexes for performance.
  </action>
  <verify>
    <automated>grep "CREATE TABLE" database/schema.sql</automated>
  </verify>
  <done>MySQL schema is defined and ready for execution.</done>
</task>

<task type="auto">
  <name>Task 2: Database Abstraction & Base Repository</name>
  <files>src/Core/Database.php, src/Core/BaseRepository.php</files>
  <action>
    1. Create `src/Core/Database.php`:
       - Singleton or simple PDO wrapper.
       - Connect via environment variables (DB_HOST, DB_NAME, DB_USER, DB_PASS).
       - Use `PDO::ERRMODE_EXCEPTION` and `PDO::ATTR_DEFAULT_FETCH_MODE => PDO::FETCH_ASSOC`.
    2. Create `src/Core/BaseRepository.php`:
       - Generic base class for all repositories.
       - Methods: `all()`, `find(int|string $id)`, `create(array $data)`, `update(int|string $id, array $data)`, `delete(int|string $id)`.
       - Inject `Database` instance in constructor.
  </action>
  <verify>
    <automated>php -l src/Core/Database.php && php -l src/Core/BaseRepository.php</automated>
  </verify>
  <done>Core database classes are implemented and syntactically correct.</done>
</task>

</tasks>

<verification>
Verify that the `Database` class correctly attempts connection via PDO and `BaseRepository` provides standard CRUD methods.
</verification>

<success_criteria>
- Schema SQL is valid.
- Database and BaseRepository classes follow PSR-4 and are properly typed (PHP 8.2+ style).
</success_criteria>

<output>
After completion, create `.planning/phases/01-architecture-foundation/01-02-SUMMARY.md`
</output>
