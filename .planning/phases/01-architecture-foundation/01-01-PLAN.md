---
phase: 01-architecture-foundation
plan: 01
type: execute
wave: 1
depends_on: []
files_modified: [composer.json, public/index.php, config/bootstrap.php, .env.example]
autonomous: true
requirements: [ARCH-01, ARCH-03]
must_haves:
  truths:
    - "Classes in src/ are autoloaded under App\\ namespace"
    - "public/index.php loads the environment via config/bootstrap.php"
  artifacts:
    - path: "composer.json"
      provides: "Project dependencies and PSR-4 config"
    - path: "config/bootstrap.php"
      provides: "Application initialization logic"
    - path: "public/index.php"
      provides: "Web entry point"
  key_links:
    - from: "public/index.php"
      to: "config/bootstrap.php"
      via: "require_once"
    - from: "config/bootstrap.php"
      to: "vendor/autoload.php"
      via: "require_once"
---

<objective>
Initialize the project with a professional PSR-4 folder structure and the primary entry points.

Purpose: Establishes the structural foundation for all subsequent feature development.
Output: Validated directory structure, composer.json with core dependencies, and functional index.php/bootstrap.php.
</objective>

<execution_context>
@C:/Users/Thanon6532/.gemini/get-shit-done/workflows/execute-plan.md
@C:/Users/Thanon6532/.gemini/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/research/STACK.md
@.planning/research/ARCHITECTURE.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Project Scaffolding & Dependencies</name>
  <files>composer.json, .env.example</files>
  <action>
    1. Create the following directory structure:
       - `config/` (Configuration files)
       - `public/` (Web root)
       - `resources/` (Assets: css, js, views)
       - `src/` (Source code: Core, Models, Repositories, Services)
       - `storage/` (Logs, cache, uploads)
       - `tests/` (Unit/Integration tests)
    2. Initialize `composer.json` with:
       - PSR-4 autoloading: `"App\\": "src/"`
       - Dependencies: `nutgram/nutgram`, `spatie/browsershot`, `openspout/openspout`, `php-flasher/flasher`.
       - Dev dependencies: `phpunit/phpunit`, `filp/whoops`, `vlucas/phpdotenv`.
    3. Create `.env.example` with placeholders for DB_*, TG_BOT_TOKEN, and GOOGLE_*.
    4. Run `composer install` (mock or actual if environment permits).
  </action>
  <verify>
    <automated>php -r "require 'vendor/autoload.php'; echo 'Autoloader works';"</automated>
  </verify>
  <done>Directory structure exists, composer.json is valid, and vendor/autoload.php is generated.</done>
</task>

<task type="auto">
  <name>Task 2: Entry Points (Bootstrap & Index)</name>
  <files>config/bootstrap.php, public/index.php</files>
  <action>
    1. Create `config/bootstrap.php`:
       - Initialize `phpdotenv` to load `.env`.
       - Setup error handling (Whoops for dev).
       - Initialize timezone (UTC).
       - Return a configured DI container or app instance (simple for now).
    2. Create `public/index.php`:
       - Require `bootstrap.php`.
       - Simple "Hello World" or app run command to verify it works.
  </action>
  <verify>
    <automated>php public/index.php | grep "Hello"</automated>
  </verify>
  <done>Application bootstraps correctly without errors.</done>
</task>

</tasks>

<verification>
Check that `composer.json` has all required dependencies and the directory structure matches the PSR-4 standard.
</verification>

<success_criteria>
- `composer validate` passes.
- `php public/index.php` executes without errors and loads the environment.
</success_criteria>

<output>
After completion, create `.planning/phases/01-architecture-foundation/01-01-SUMMARY.md`
</output>
